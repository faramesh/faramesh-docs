---
title: Which CLI commands does Faramesh expose?
---

Core: `init`, `check`, `plan`, `apply`, `status`, `destroy`

Utilities: `test`, `explain`, `rollback`

Groups: `approvals`, `audit`, `credential`, `agent`

Dev: `dev`

`apply` always runs `check` first.

Next: [Dev mode](/dev/).
